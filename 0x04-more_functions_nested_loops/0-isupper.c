/**
 * _isupper - Program to check if a character is in lower case or not
 * @c: the parameter to be checked in the function
 *
 * Return: Always 0 (Success)
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
